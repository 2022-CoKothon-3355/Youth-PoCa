import 'package:flutter/material.dart';

class CommonFormCard extends StatelessWidget {
  const CommonFormCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
